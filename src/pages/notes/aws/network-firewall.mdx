import { ArticleLayout } from "@/components/ArticleLayout";

export const meta = {
  author: "Rocky Warren",
  date: "2022-11-25",
  title: "Artifact",
  description: "",
};

export default (props) => <ArticleLayout meta={meta} {...props} />;

- Centralized policy-based management and consolidated billing
- After creating service control policies (SCP), must associate with accounts/organizational units (OU) before they take effect

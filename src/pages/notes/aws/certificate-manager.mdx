import { ArticleLayout } from "@/components/ArticleLayout";

export const meta = {
  author: "Rocky Warren",
  date: "2022-11-26",
  title: "Certificate Manager (ACM)",
  description: "",
};

export default (props) => <ArticleLayout meta={meta} {...props} />;

- Handles complexity of creating, storing, and renewing public and private TLS X.509 certificates and keys

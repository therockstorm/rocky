import { ArticleLayout } from "@/components/ArticleLayout";

export const meta = {
  date: "2020-05-25",
  title: "Macie",
  description: "",
};

export default (props) => <ArticleLayout meta={meta} {...props} />;

- Uses AI to analyze S3 and CloudTrail data to identify security issues (private keys, etc.), financial information, and personally identifiable information (PII)
- Can create custom regex
- Scheduled (daily, monthly) or one-time scans

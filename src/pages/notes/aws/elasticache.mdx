import { ArticleLayout } from "@/components/ArticleLayout";

export const meta = {
  date: "2020-05-25",
  title: "ElastiCache",
  description: "",
};

export default (props) => <ArticleLayout meta={meta} {...props} />;

- Increase database and web application performance with caching
- Supports Memcached and Redis (supports backups and multi-AZ)

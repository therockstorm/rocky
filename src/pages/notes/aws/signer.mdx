import { ArticleLayout } from "@/components/ArticleLayout";

export const meta = {
  date: "2022-11-26",
  title: "AWS Signer",
  description: "",
};

export default (props) => <ArticleLayout meta={meta} {...props} />;

- Managed code-signing service to ensure trust and integrity of your Lambda and IoT code
- Validate code against digital signature to confirm it's unaltered and from trusted publisher

import { ArticleLayout } from "@/components/ArticleLayout";

export const meta = {
  author: "Rocky Warren",
  date: "2020-05-25",
  title: "API Gateway",
  description: "",
};

export default (props) => <ArticleLayout meta={meta} {...props} />;

- Gateway into AWS resources
- Provides caching, request validation, logging, CORS support, rate-limiting, and auto-scaling

import { ArticleLayout } from "@/components/ArticleLayout";

export const meta = {
  author: "Rocky Warren",
  date: "2020-05-25",
  title: "X-Ray",
  description: "",
};

export default (props) => <ArticleLayout meta={meta} {...props} />;

- Trace requests through multiple AWS services

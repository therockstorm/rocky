import { ArticleLayout } from "@/components/ArticleLayout";

export const meta = {
  date: "2022-11-21",
  title: "Config",
  description: "",
};

export default (props) => <ArticleLayout meta={meta} {...props} />;

- Record resource config changes over time.
- Custom and managed rules/conformance pack best practices/regulations to continuously monitor accounts and report findings.
- Must enable global resource checks if, e.g., IAM is desired
